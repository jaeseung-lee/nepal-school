## Imported Claude Cowork project instructions

## 외부 문의 이력과 중복 발송 방지

- 학교·기관·담당자에게 이메일이나 문의 폼으로 연락하기 전에 `docs/outreach-records.md`와 `output/outreach/contact-register.json`을 확인한다. 발신 계정이나 연락 채널이 달라도 같은 수신처의 이력으로 취급한다.
- `python3 scripts/check-outreach.py "학교명" "이메일 또는 문의 폼 URL"`로 기존 기록을 조회한다. 학교의 한국어·일본어 이름, 다른 이메일, 다른 언어의 문의 폼도 같은 기관에 연결해서 확인한다. 담당자 실명이 확인되지 않았다면 학교·기관 단위로 중복을 막는다.
- `contacted`인 대상에게 같은 최초 소개·협업 문의를 다시 보내지 않는다. 사용자가 해당 대상에 대한 후속 연락이나 재발송을 명시적으로 요청한 경우에는 그 지시를 따르고 기존 이력에 추가한다. 이미 허가한 동일한 후속 연락에 대해 확인을 반복해서 요구하지 않는다.
- `needs_review` 또는 결과 불명·전송 중·기존 초안 기록은 새 연락 대상으로 간주하지 않는다. 보낸편지함·기존 초안·접수 결과를 확인한 뒤 처리한다. 조회 결과가 없다는 것만으로 발송이 허가되지는 않는다.
- 새로운 발송 시도는 외부 전송 전에 명부에 `needs_review` 상태로 기록한다. 발송·접수 확인 후에만 `contacted`로 변경하고 날짜, 실제 채널, 수신처, 발신·회신 주소, 문의 내용, 확인 근거를 추가한다. 실패·취소·결과 불명 이력도 보존한다. 새로운 별칭·담당자 이메일은 기존 기관 항목에 추가한다.
- 명부의 JSON이 기준 데이터이며 Markdown/CSV 요약도 함께 갱신한다. 명부를 찾을 수 없거나 읽을 수 없으면 신규 대상으로 판정하지 않는다. 발송 이력·개인 연락처는 공개 사이트나 공개 저장소에 게시하지 않는다.
